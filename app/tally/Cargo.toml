[package]
name = "tally"
version = "0.1.0"
edition = "2024"
description = "Tally — plain-text, double-entry accounting for the terminal"

[[bin]]
name = "tally"
path = "src/main.rs"

[dependencies]
tally-core = { path = "../core" }

[lints]
workspace = true
